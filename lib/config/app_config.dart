class AppConfig {
  // Cambia esta URL por la base de tu API
  static const String baseUrl = 'https://tu-backend.com/api';

  // Timeout de peticiones en segundos
  static const int connectTimeout = 10;
  static const int receiveTimeout = 10;
}
